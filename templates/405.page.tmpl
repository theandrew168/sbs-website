{{template "base" .}}

{{define "main"}}
<!-- main content -->
<main class="flex-grow">
	<div class="container mx-auto px-8 mt-8 lg:mt-16">
		<!-- content -->
		<p class="text-xl">Method not allowed!</p>
	</div>
</main>
{{end}}
